export function astar(pass_map, vis_map, start, end) {
  // bruh, it's just an astar
  return [path] // the entire path, from start -> stop, as node objects. nodes have a .x and a .y
  return []; // no move
}